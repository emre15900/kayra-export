{
  "private": true,
  "workspaces": [
    "home",
    "cart",
    "shared"
  ],
  "name": "kayra-export",
  "version": "1.0.0",
  "description": "Micro-frontend product and cart application",
  "scripts": {
    "dev": "concurrently \"yarn workspace home dev\" \"yarn workspace cart dev\"",
    "build": "yarn workspace home build && yarn workspace cart build",
    "lint": "yarn workspace home lint && yarn workspace cart lint"
  },
  "devDependencies": {
    "concurrently": "^9.2.0"
  }
}
